This is the html and the css for the front end for the submit page
Paste this in index.html
--- Paste statrting from here ---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>YouTube Summarizer</title>

  <!-- Fonts from your design -->
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700&family=Oswald:wght@600&display=swap" rel="stylesheet">

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: #f5f5f5;
      font-family: 'Plus Jakarta Sans', sans-serif;
    }

    /* Main Card */
    .note-entry {
      width: 800px;
      max-width: 95%;
      background: rgba(255, 255, 255, 0.9);
      border-radius: 20px;
      padding: 48px;
    }

    /* Header */
    .header {
      display: flex;
      align-items: center;
      gap: 20px;
      margin-bottom: 40px;
    }

   .yt-icon {
  width: 79.5px;
  height: 53px;
  background: #ff0000;
  border-radius: 18px;

  display: flex;
  align-items: center;
  justify-content: center;

  box-shadow: 0 6px 16px rgba(0,0,0,0.2);
}




    .title {
      font-family: 'Oswald', sans-serif;
      font-size: 56px;
      font-weight: 600;
      color: #000;
    }

    /* Form layout */
    form {
      display: flex;
      flex-direction: column;
      gap: 28px;
    }

    /* Inputs */
    input[type="text"],
    select,
    textarea {
      width: 100%;
      border: 0.8px solid #D11414;
      border-radius: 10px;
      padding: 18px;
      font-size: 16px;
      font-family: 'Plus Jakarta Sans', sans-serif;
    }

    input::placeholder,
    textarea::placeholder {
      color: #8D8D8D;
    }

    textarea {
      min-height: 180px;
      resize: vertical;
      font-size: 20px;
      line-height: 1.4;
    }

    select {
      appearance: none;
      background-image:
        linear-gradient(45deg, transparent 50%, black 50%),
        linear-gradient(135deg, black 50%, transparent 50%);
      background-position:
        calc(100% - 20px) calc(50% - 3px),
        calc(100% - 14px) calc(50% - 3px);
      background-size: 6px 6px;
      background-repeat: no-repeat;
    }

    /* Button */
    .submit-wrapper {
      display: flex;
      justify-content: center;
      margin-top: 10px;
    }

    button {
      width: 300px;
      height: 58px;
      border-radius: 54px;
      border: none;
      background: linear-gradient(90deg, #FF0000 0%, #AD342B 97%);
      box-shadow: 0px 8px 24px rgba(22, 28, 45, 0.1);
      color: white;
      font-size: 16px;
      font-weight: 700;
      cursor: pointer;
    }

    .play-icon {
     width: 48px;
  height: 48px;
  fill: white;

  /* Optical centering */
  transform: translateX(-2px);
}


    button:hover {
      opacity: 0.9;
    }

    @media (max-width: 600px) {
      .title {
        font-size: 40px;
      }

      textarea {
        font-size: 16px;
      }
    }
  </style>
</head>

<body>

  <div class="note-entry">

    <!-- Header -->
    <div class="header">
      <div class="yt-icon">
  <svg class="play-icon" viewBox="0 0 24 24">
    <polygon points="9,7 19,12 9,17"></polygon>
  </svg>
</div>


      <div class="title">YouTube Summarizer</div>
    </div>

    <!-- Form -->
    <form action="/summarize" method="post" target="_blank">

      <input
        type="text"
        name="youtube_link"
        placeholder="Paste a YouTube link here"
        required>

      <select name="model">
        <option value="gemini-2.0-flash-001">Gemini 2.0 Flash</option>
      </select>

    <textarea
  name="additional_prompt"
  placeholder="Write your additional prompt here. For example: 'Explain to me like I just learnt my ABC’s.'">
</textarea>


      <div class="submit-wrapper">
        <button type="submit">Summarize</button>
      </div>

    </form>

  </div>

</body>
</html>
